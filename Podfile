use_frameworks!

pod 'Particle-SDK', :path => '~/Code/Particle-SDK'
pod 'SwiftyJSON'
plugin 'cocoapods-keys', {
  :project => "WaterClosetMenu",
  :keys => [
    "OAuthToken",
    "ParticleUser",
    "ParticlePassword"
  ]}
